---
layout: layouts/base.njk
---

<h1 id="main-content" tabindex="-1">{{ title }}</h1>

{{ content | safe }}

<h2>Contributions by {{ title }}</h2>

<ol>
    {% for post in collections.piece | authorIncludes(code_name) | reverse %}
        {% include "partials/post-item.html" %}                  
    {% endfor %}
</ol>